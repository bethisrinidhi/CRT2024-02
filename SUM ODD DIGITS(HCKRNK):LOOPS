SUM OF ODD DIGITS IN A NUM:(HACKERRANK)-LOOPS

#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,r,sum=0;
    scanf("%d",&n);
    while(n>=1){
        r=n%10;
        n=n/10;
        if(r%2!=0){
            sum=sum+r;
        }
    }
    printf("%d",sum);
    return 0;
}
