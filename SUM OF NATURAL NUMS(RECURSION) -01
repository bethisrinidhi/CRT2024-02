SUM OF n-NATURAL NUMS 01:


#include <stdio.h>
int sum(int n){
     if(n==0)
      return 0;
      else
      return n+sum(n-1);
}
    int main(){
        int n;
        scanf("%d",&n);
        int r=sum(n);
        printf("%d",r);
        return 0;
    }
